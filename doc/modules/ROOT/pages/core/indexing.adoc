= Indexing Classes

* ways
    ** -- via -- API
        *** create a Persistent Index
        *** load the PREVIOUS index
    ** -- via -- CLI
        *** 's output information == number of classes + number of annotation declarations + size
    ** -- via -- Ant Task
    ** -- via -- xref:maven/basic.adoc[Maven Plugin]
